package com.owen.offer;

public class FindValueInArray
{
    public static void main(String[] args)
    {
    }

    private static boolean hasValue(int[][] array, int value)
    {
//        assert array != null && array.length > 0 && array[0].length > 0;
//        for(int i = )
        return true;
    }
}
